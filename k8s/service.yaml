apiVersion: v1
kind: Service
metadata:
  name: weather-app-service
  labels:
    app: weather-app
spec:
  selector:
    app: weather-app
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP